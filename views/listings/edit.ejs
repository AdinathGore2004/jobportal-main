<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Job</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <style>
        body {
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            background-color: #f8f9fa;
        }
        .card {
            width: 100%;
            max-width: 500px;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }
    </style>
</head>
<body>

    <div class="card">
        <h2 class="text-center mb-3">Edit Job</h2>
        <form action="/update-job/<%= job.id %>" method="POST">
            <div class="mb-3">
                <input type="text" class="form-control" name="title" value="<%= job.title %>" required>
            </div>
            <div class="mb-3">
                <textarea class="form-control" name="description" rows="4" required><%= job.description %></textarea>
            </div>
            <div class="mb-3">
                <input type="text" class="form-control" name="company_name" value="<%= job.company_name %>" readonly required>
            </div>
            <div class="mb-3">
                <input type="text" class="form-control" name="location" value="<%= job.location %>" required>
            </div>
            <div class="mb-3">
                <input type="text" class="form-control" name="salary" value="<%= job.salary %>" required>
            </div>
            <div class="mb-3">
                <select class="form-control" name="job_type" required>
                    <option value="Full-time" <%= job.job_type === 'Full-time' ? 'selected' : '' %>>Full-time</option>
                    <option value="Part-time" <%= job.job_type === 'Part-time' ? 'selected' : '' %>>Part-time</option>
                    <option value="Internship" <%= job.job_type === 'Internship' ? 'selected' : '' %>>Internship</option>
                    <option value="Contract" <%= job.job_type === 'Contract' ? 'selected' : '' %>>Contract</option>
                </select>
            </div>
            <div class="mb-3">
                <input type="text" class="form-control" name="experience_required" value="<%= job.experience_required %>" required>
            </div>
            <div class="mb-3">
                <input type="date" class="form-control" name="application_deadline" value="<%= job.application_deadline.toISOString().split('T')[0] %>" required>
            </div>
            <div class="mb-3">
                <input type="url" class="form-control" name="apply_link" value="<%= job.apply_link %>" required>
            </div>
            <button type="submit" class="btn btn-primary w-100">Update Job</button>
        </form>
    </div>

</body>
</html>
